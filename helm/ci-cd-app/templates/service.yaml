apiVersion: v1
kind: Service
metadata:
  name: ci-cd-service
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ci-cd-app
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 5000