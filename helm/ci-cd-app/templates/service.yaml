apiVersion: v1
kind: Service
metadata:
  name: ci-cd-service
  labels:
    app: ci-cd-app
spec:
  type: {{ .Values.service.type }}
  selector:
    app: ci-cd-app
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: 5000